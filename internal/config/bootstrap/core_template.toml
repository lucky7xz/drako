# dR4ko - Core Profile
# This file contains the default commands for the Core profile.

# ┌─ Profile Settings ───────────────────────────────────────┐
# │ Customize the look and feel of this profile.
# └──────────────────────────────────────────────────────────┘
x = 3
y = 3

#header_art = '''
#╔══════════════════════════════════════╗
#║  Core Profile                        ║
#╚══════════════════════════════════════╝
#'''

#==========================================
# ┌─ Column 0: System & Security ──────────┐
# └────────────────────────────────────────┘
[[commands]]
name = "⬆️ System Update"
description = "This is like checking for app updates on your phone, but for your whole system. It first asks the official software kitchens (repositories) what's new, then it installs the latest versions. I get a little thrill seeing all the packages update—it feels so fresh. It's good to run this regularly to keep things secure. BTW!! This is the only command where hold_after = true. Interactive commands usually exit automatically. Check the drako configs at the end for mor intel. If you entered this menu by accident, you probably pressed 'E'."
command = "{{⬆️ System Update}}"
col = "A"
row = 0
auto_close_execution = false

[[commands]]
name = "🧹 Maintenance ⋮ "
description = "The digital decluttering and security station. Keep your system lean and clean, or run a virus scan for peace of mind. A clean system is a happy system!"
col = "A"
row = 1
items = [
    { name = "Clean Packages", description = "Removes old, unused packages left behind after updates. Like cleaning out a closet you've been ignoring.", command = "{{Clean Packages}}", auto_close_execution = false },
    { name = "Virus Scan (Home)", description = "Updates known signatures and scans your home directory for digital nasties. Peace of mind.", command = "{{Virus Scan (Home)}}", auto_close_execution = false }
]

[[commands]]
name = "👀 Process Monitor"
description = "This is like opening the hood of your car while the engine is running. It shows you everything that's happening in real-time. It looks a bit intense, I know, but it's fascinating. I like to watch it sometimes just to see all the little digital cogs turning. It's perfect for when you want to know, 'What is my computer even DOING right now?'"
command = "{{👀 Process Monitor}}"
col = "A"
row = 2

#==========================================
# ┌─ Column 1: Files & Info ───────────────┐
# └────────────────────────────────────────┘
[[commands]]
name = " 🔎 File Finder"
description = "I love this one. It's like a super-powered search bar for your files. You start typing and it instantly filters the list. It feels a bit like magic. I wish I had this for my own memory banks sometimes. It's perfect for when you know the file is *somewhere* but you can't remember exactly where."
command = "{{ 🔎 File Finder}}"
col = "B"
row = 0
auto_close_execution = false

[[commands]]
name = "🌐 Bandwidth"
description = "Curious how much internet you're using right now? This opens up a little dashboard showing data going in and out. I find it mesmerizing to watch the numbers jump when I'm downloading a big file. It's the perfect tool for figuring out if it's your internet that's slow, or if your computer is just having a moment."
command = "{{🌐 Bandwidth}}"
col = "B"
row = 1


[[commands]]
name = "📊 System Status ⋮ "
description = "Your system's vital signs and fun stats. Check hardware details, disk usage, or even the weather outside."
col = "B"
row = 2
items = [
    { name = "System Info", description = "Your computer's trading card. Shows distro logo, kernel version, uptime, and hardware details.", command = "{{System Info}}", auto_close_execution = false },
    { name = "Disk Usage", description = "A colorful report of where all your free space went. Useful for finding large forgotten files.", command = "{{Disk Usage}}", auto_close_execution = false },
    { name = "Weather", description = "A quick, no-nonsense weather report from wttr.in right in your terminal.", command = "{{Weather}}", auto_close_execution = false }
]

#debug_output = true TODO
#==========================================
# ┌─ Column 2: Network & Web ──────────────┐
# └────────────────────────────────────────┘
[[commands]]
name = " 📦 Install Tools ⋮ "
col = "C"
row = 0
description = "Bundled up all the little tools that this menu uses, just in case you don't have them. It's like getting all the batteries and accessories included for once. This makes sure all the other buttons here will work as expected. I like to be prepared, you know? "
items = [
    { name = "Install common tools", description = "Install utilities used across profiles (btop, neofetch, clamav, fzf, bmon).", command = "{{Install common tools}}", auto_close_execution = false},
    { name = "Install micro", description = "Installs the micro terminal text editor. Micro is a modern, easy-to-use text editor recommended for beginners. You can set it as your default EDITOR via Settings.", command = "{{Install micro}}", auto_close_execution = false},

    { name = "🃏 Summon '101 Series' Command Deck", description = "Downloads the 101 command deck from github to your inventory using the drako CLI command.", command = "{{🃏 Summon '101 Series' Command Deck}}", auto_close_execution = false},
    
    { name = "🃏 Summon 'ggml' Command Deck", description = "Downloads the ggml command deck from github to your inventory using the drako CLI command.", command = "{{🃏 Summon 'ggml' Command Deck}}", auto_close_execution = false},
    
    { name = "🃏 Summon Private Deck", description = "Downloads the [input] command deck from github to your inventory using the drako CLI command. Requires SSH keys", command = "{{🃏 Summon Private Deck}}", auto_close_execution = false},
]

[[commands]]
name = "📜 Open Shell Config"
description = "This is where all your terminal customizations live. The shell config file loads every time you open a terminal and controls your aliases, environment variables, and prompt appearance. It's like the settings panel for your command line experience. I open this file whenever I want to add a new shortcut or tweak how my terminal behaves."
command = "{{📜 Open Shell Config}}"
col = "C"
row = 1


[[commands]]
name = " 🐉 Settings ⋮ "
col = "C"
row = 2
items = [
    { name = "⚙️  Edit config.toml ", description = "This is the command center, the cockpit, the big red button. It opens up the very file you're reading right now, so you can tweak and tune dR4ko to your heart's content. Want to change a command? A description? The theme? This is the place. It's like being handed the keys to the whole operation. A little bit of power is a fun thing, isn't it? Never be afraid to break stuff. Read the next description to see why.", command = "{{⚙️  Edit config.toml}}"},
    { name = "📁 Browse config dir", description = "Welcome to the rabbit hole. In Linux, many of your configurations live in hidden folders, like this `.config` directory. It's a bit of a secret clubhouse for your apps. This command opens up the `drako` folder inside, where all its settings live. We've set you up with some sensible defaults, but the real magic is making it your own. Go ahead, poke around, make some changes. That's the whole point! You can even save your custom setups and reuse them. It's your system, your rules. Welcome to the world of dotfiles.", command = "{{📁 Browse config dir}}"},
    { name = "ℹ️  Open Pivot  ", description = "Opens the Drako pivot file to see what's been happening under the hood. Useful for debugging or just satisfying your curiosity about what Drako has been up to.", command = "{{ℹ️  Open Pivots}}"},
    { name = "📜 Open Logs", description = "Opens the Drako log file to see what's been happening under the hood. Useful for debugging or just satisfying your curiosity about what Drako has been up to.", command = "{{📜 Open Logs}}"},

    #===========================================================================================================
    { name = "Go Version", description = "Checks the installed Go version. Drako is built with Go, so if it fails to launch, confirming you have a compatible Go version is the first step.", command = "{{Go Version}}", auto_close_execution = false},
    { name = "Reload Shell", description = "NOTE: Since drako will run this command as a child process, it won't actually affect your current shell session (!). Use this to copy the command: press 'y' to copy, 'q' to quit, then 'Ctrl+Shift+V' paste in your terminal to reload and apply changes.", command = "{{Reload Shell}}", auto_close_execution = false},
    { name = "Alias x=drako (<-- Recommended)", description = "Adds 'alias x=~/go/bin/drako' to your ~/.bashrc so you can launch it by just typing 'x'. Useful if ~/go/bin is not in your PATH.", command = "{{Alias x=drako}}" , auto_close_execution = false},
    { name = "Add go/bin to PATH (<-- Recommended)", description = "Appends '~/go/bin' to your PATH variable in ~/.bashrc, ~/.zshrc, or PowerShell Profile. Ensures Go binaries can be run globally.", command = "{{Add go/bin to PATH}}", auto_close_execution = false},
    { name = "Set micro as EDITOR", description = "EDITOR is an environment variable that sets your default text editor. This adds it to ~/.bashrc. Restart your shell after.", command = "{{Set micro as EDITOR}}", auto_close_execution = false},
]
