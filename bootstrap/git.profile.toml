# The Git Commando Grid - Your Daily Version Control Workspace
# Forged for efficient, AI-assisted software development and collaboration.

# ┌─ Grid Dimensions ──────────────────────────────┐
X = 4
Y = 4
# └────────────────────────────────────────────────┘

#=======================================================================================================================
# ┌─ COLUMN A: Local Changes ──────────────────────────────────────────────────────────────────────────────────────────┐
# │ Core commands for managing your work in the local repository. From checking status to committing changes.
# └─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

[[commands]]
name = "📝 Check Status"
description = "Quickly see which files have been modified, which are staged, and which are untracked. Your command center for local changes."
col = "A"
row = 0
items = [
    { name = "Full Status", command = "git status", auto_close_execution = false },
    { name = "Short Status", command = "git status -s", auto_close_execution = false }
]

[[commands]]
name = "📥 Stage Changes"
description = "Prepare your modified files for a commit. Staging lets you craft precise, atomic commits."
col = "A"
row = 1
items = [
    { name = "Stage All", description = "Stages all modified and new files in the current directory.", command = "git add .", auto_close_execution = false },
    { name = "Interactive Staging", description = "Allows you to interactively choose which changes to stage. Perfect for splitting a large change into smaller commits.", command = "git add -p", auto_close_execution = false }
]

[[commands]]
name = "💾 Commit Changes"
description = "Save your staged changes to the project's history. A commit is a snapshot of your work at a specific point in time."
col = "A"
row = 2
items = [
    { name = "Commit Staged", command = "read -p 'Enter commit message: ' msg; git commit -m \"$msg\"", auto_close_execution = false },
    { name = "Amend Last Commit", description = "Modify the last commit with new staged changes or a new message. Useful for fixing typos or adding forgotten files.", command = "git commit --amend", auto_close_execution = false }
]

[[commands]]
name = "🚀 Workflow Shortcuts"
description = "Sensible command chains to speed up your daily workflow."
col = "A"
row = 3
items = [
    { name = "Add & Commit", description = "A convenient shortcut to stage all changes and commit them with a single message.", command = "git add . && read -p 'Enter commit message: ' msg; git commit -m \"$msg\"", auto_close_execution = false }
]


#=======================================================================================================================
# ┌─ COLUMN B: Branching & History ────────────────────────────────────────────────────────────────────────────────────┐
# │ Create, navigate, and understand the history of your project with branches.
# └─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

[[commands]]
name = "🌿 Manage Branches"
description = "View and manage the different lines of development in your repository."
col = "B"
row = 0
items = [
    { name = "List All Branches", description = "Shows all local and remote branches.", command = "git branch -a", auto_close_execution = false },
    { name = "Delete a Local Branch", command = "read -p 'Enter local branch name to delete: ' branch; git branch -d \"$branch\"", auto_close_execution = false }
]

[[commands]]
name = "🔁 Create & Switch"
description = "Create new branches to work on features or fixes in isolation and switch between them."
col = "B"
row = 1
items = [
    { name = "Create New Branch", command = "read -p 'Enter new branch name: ' branch; git branch \"$branch\"", auto_close_execution = false },
    { name = "Switch Branch", description = "Switch your working directory to a different branch.", command = "read -p 'Enter branch name to switch to: ' branch; git checkout \"$branch\"", auto_close_execution = false },
    { name = "Create & Switch", description = "A handy shortcut to create a new branch and immediately switch to it.", command = "read -p 'Enter new branch name: ' branch; git checkout -b \"$branch\"", auto_close_execution = false }
]

[[commands]]
name = "🧬 Merge & Rebase"
description = "Integrate changes from one branch into another. Merging creates a new commit, while rebasing rewrites history."
col = "B"
row = 2
items = [
    { name = "Merge Branch", description = "Merge changes from another branch into your current branch.", command = "read -p 'Enter branch to merge in: ' branch; git merge \"$branch\"", auto_close_execution = false },
    { name = "Rebase Branch", description = "Re-apply commits from your current branch onto another branch. Creates a cleaner, linear history.", command = "read -p 'Enter branch to rebase onto: ' branch; git rebase \"$branch\"", auto_close_execution = false }
]

[[commands]]
name = "📜 View History"
description = "Inspect the project's commit history to understand how it has evolved."
col = "B"
row = 3
items = [
    { name = "Pretty Log", description = "A compact and graphical view of the commit history.", command = "git log --oneline --graph --decorate --all", auto_close_execution = false },
    { name = "Detailed Log", description = "Shows the full commit history with author, date, and message.", command = "git log", auto_close_execution = false }
]


#=======================================================================================================================
# ┌─ COLUMN C: Remote Operations ──────────────────────────────────────────────────────────────────────────────────────┐
# │ Collaborate with others by synchronizing your local repository with a remote one (like on GitHub).
# └─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

[[commands]]
name = "📡 View Remotes"
description = "Manage your connections to remote repositories."
col = "C"
row = 0
items = [
    { name = "List Remotes", command = "git remote -v", auto_close_execution = false },
    { name = "Add Remote", command = "read -p 'Enter remote name (e.g., origin): ' name; read -p 'Enter remote URL: ' url; git remote add \"$name\" \"$url\"", auto_close_execution = false }
]

[[commands]]
name = "⏬ Fetch & Pull"
description = "Download changes from a remote repository to your local machine."
col = "C"
row = 1
items = [
    { name = "Fetch All", description = "Downloads all new data from all remotes without integrating it into your working files.", command = "git fetch --all --prune", auto_close_execution = false },
    { name = "Pull Current Branch", description = "Fetches changes from the remote and merges them into your current branch.", command = "git pull", auto_close_execution = false }
]

[[commands]]
name = "⏫ Push Changes"
description = "Upload your local commits to a remote repository to share them with others."
col = "C"
row = 2
items = [
    { name = "Push Current Branch", description = "Uploads your current branch to its upstream remote.", command = "git push", auto_close_execution = false },
    { name = "Push New Branch", description = "Pushes a new local branch to the remote and sets it up to track.", command = "git push -u origin $(git rev-parse --abbrev-ref HEAD)", auto_close_execution = false }
]


#=======================================================================================================================
# ┌─ COLUMN D: Utility & Advanced ─────────────────────────────────────────────────────────────────────────────────────┐
# │ Useful tools for comparing changes, temporarily saving work, and other advanced tasks.
# └─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

[[commands]]
name = "👀 View Diffs"
description = "See the exact changes between commits, branches, or your working directory and the index."
col = "D"
row = 0
items = [
    { name = "Diff Unstaged", description = "Shows changes in your working directory that are not yet staged.", command = "git diff", auto_close_execution = false },
    { name = "Diff Staged", description = "Shows changes that are staged but not yet committed.", command = "git diff --staged", auto_close_execution = false }
]

[[commands]]
name = "📦 Stash"
description = "Temporarily shelve your modified, tracked files in order to switch branches or work on something else."
col = "D"
row = 1
items = [
    { name = "Stash Changes", description = "Temporarily shelve your modified, tracked files.", command = "git stash", auto_close_execution = false },
    { name = "List Stashes", description = "Lists all your stashed changes.", command = "git stash list", auto_close_execution = false },
    { name = "Apply Stash", description = "Applies the most recent stash without removing it from the list.", command = "git stash apply", auto_close_execution = false },
    { name = "Pop Stash", description = "Applies the most recent stash and removes it from the list.", command = "git stash pop", auto_close_execution = false }
]
